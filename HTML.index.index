<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Hey Yawie ❤️💍</title>
  <style>
    body {
      background-color: #ffe6eb;
      font-family: "Poppins", sans-serif;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      text-align: center;
      overflow: hidden;
    }
    h1 {
      font-size: 2em;
      color: #ff4d6d;
      margin-bottom: 10px;
    }
    p {
      font-size: 1.2em;
      color: #444;
      margin-bottom: 25px;
    }
    .btn {
      padding: 14px 30px;
      margin: 10px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1.1em;
      transition: all 0.3s ease;
    }
    .yes {
      background-color: #ff4d6d;
      color: white;
    }
    .no {
      background-color: #ccc;
      color: #333;
      position: absolute;
    }
    .footer {
      position: absolute;
      bottom: 20px;
      font-size: 1em;
      color: #ff4d6d;
    }
  </style>
</head>
<body>
  <h1>Hey Yawie ❤️💍</h1>
  <p>Would you go on a date with me?</p>

  <div>
    <button class="btn yes" onclick="sayYes()">Yes 😍</button>
    <button class="btn no" onmouseover="moveButton(this)" ontouchstart="moveButton(this)">No 🙈</button>
  </div>

  <div class="footer">From Gavin 💖</div>

  <script>
    function moveButton(btn) {
      const x = Math.random() * (window.innerWidth - btn.offsetWidth - 20);
      const y = Math.random() * (window.innerHeight - btn.offsetHeight - 20);
      btn.style.left = `${x}px`;
      btn.style.top = `${y}px`;
    }

    function sayYes() {
      document.body.innerHTML = "<h1 style='color:#ff4d6d; text-align:center;'>Yay! ❤️ Can't wait 😘</h1>";
    }
  </script>
</body>
</html>
